[package]
name = "ui"
version = "0.1.0"
edition = "2021"

# The UI crate is the egui control surface. It owns the inspector and palette state,
# reads the authored document, and emits Intents. It does not depend on rendering or
# GPU resources; the shell supplies registered thumbnail TextureIds and owns drawing.
# The dependency edge is one-way: `{document, voxel_core, camera} <- ui <- shell`.

[lib]
name = "ui"
path = "src/lib.rs"

[dependencies]
# Authored block/voxel measurements, dimension algebra, and exact-rational expressions.
parametric = { path = "../parametric" }
# The immediate-mode GUI toolkit this crate's widgets are written against. Matched to the
# workspace version so an `egui::Ui` / `egui::TextureId` crosses the shell boundary without a
# conversion. NO egui-wgpu here — the tiles reference thumbnails only as registered TextureIds.
egui = "0.34.3"
# The authored-TRUTH layer the panel reads + mutates through Intents: the scene graph, the
# producers (SdfShape / sketch solid), the 2D sketch, the Intent boundary, the units core.
document = { path = "../document" }
# The foundational value vocabulary: MaterialChoice / BlockId (the palette resolver + material
# selector) and the ShapeKind / units the inspector edits.
voxel_core = { path = "../voxel_core" }
# The pure math substrate for placement orientation.
substrate = { path = "../substrate" }
# The vector algebra used for placement rotation previews and inverse-rotation uniforms.
glam = "0.33.1"
# Serde derive for session-durable placement snap settings.
serde = { version = "1", features = ["derive"] }
# The viewing library: the ProjectionMode the camera-section toggle drives.
camera = { path = "../camera" }
# The state-classification vocabulary. `PanelState` declares which persistence artifacts receive
# each field, and the derive rejects fields that have not been classified.
snapshot = { path = "../snapshot" }
